#!/usr/bin/env bash

file="${1:-/dev/null}"

if [[ $XDG_SESSION_TYPE == "x11" ]]; then
	xclip -sel clip -t image/png -o | tee "$file"
elif [[ $XDG_SESSION_TYPE == "wayland" ]]; then
	wl-paste -t image/png  | tee "$file"
fi
